New DAG Name: StructuredDataToInfoArchive

Network:

dbname="RND",
                user="postgres",
                password="postgres",
                host="AVD:IP",
                port="5432",

Localhost:
dbname="RND",
                user="postgres",
                password="admin@12345",
                host="localhost",
                port="5432",

Load the following - into the table
SQL Query: SELECT id, dag_id as ProcessName , run_id as InstanceId, execution_date, records_processed ,  records_failed
FROM public.dag_run_metrics;



Create Table script

-- Table: public.dag_run_metrics

-- DROP TABLE IF EXISTS public.dag_run_metrics;

CREATE TABLE IF NOT EXISTS public.dag_run_metrics
(
    id bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY ( INCREMENT 1 START 1 MINVALUE 1 MAXVALUE 9223372036854775807 CACHE 1 ),
    dag_id character varying(250) COLLATE pg_catalog."default" NOT NULL,
    run_id character varying(250) COLLATE pg_catalog."default" NOT NULL,
    execution_date timestamp with time zone NOT NULL,
    records_processed bigint NOT NULL DEFAULT 0,
    records_successful bigint NOT NULL DEFAULT 0,
    records_failed bigint NOT NULL DEFAULT 0,
    notes text COLLATE pg_catalog."default",
    reserved1 text COLLATE pg_catalog."default",
    reserved2 text COLLATE pg_catalog."default",
    reserved3 text COLLATE pg_catalog."default",
    reserved4 text COLLATE pg_catalog."default",
    reserved5 text COLLATE pg_catalog."default",
    recorded_at timestamp with time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT dag_run_metrics_pkey PRIMARY KEY (id),
    CONSTRAINT uq_dagrunmetrics UNIQUE (dag_id, run_id)
)

TABLESPACE pg_default;

ALTER TABLE IF EXISTS public.dag_run_metrics
    OWNER to postgres;